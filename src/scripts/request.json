{
  "node_type": {
    "node_type_id": "jarvis.core.echo",
    "version": "0.3.1",
    "kind": "atomic",
    "description": "Echo inputs back to the caller.",
    "input_schema": { "type": "object", "additionalProperties": true },
    "output_schema": { "type": "object", "additionalProperties": true }
  }
}
